# 🔧 Troubleshooting Guide

## ✅ สิ่งที่แก้ไขแล้ว:

### 1. API Timeout
- **เพิ่ม timeout จาก 3 วินาทีเป็น 10 วินาที** (global)
- **เพิ่ม timeout สำหรับ OTP request เป็น 15 วินาที** (เฉพาะ OTP)
- แก้ปัญหา timeout ก่อนที่ email จะส่งสำเร็จ

### 2. Error Logging
- **ลด error logging สำหรับ expected 401 errors** (เมื่อยังไม่ได้ login)
- Error 401 สำหรับ `/api/auth/me` จะไม่แสดงใน console อีกต่อไป

### 3. Gmail SMTP
- **อัพเดท App Password เป็น 16 ตัวอักษร** (`rnrdrhqtbddstknl`)
- OTP email ส่งสำเร็จแล้ว (เห็นใน terminal: `✅ OTP email sent to worawich_y@cmu.ac.th`)

## 🔍 ตรวจสอบว่าแก้ไขสำเร็จ:

### 1. Frontend Console (F12)
- **ไม่ควรเห็น error 401 สำหรับ `/api/auth/me`** อีกต่อไป
- Error อื่นๆ ที่เห็น (WebSocket, Vite HMR) เป็นเรื่องปกติสำหรับ production environment

### 2. Backend Terminal
- **ไม่ควรเห็น error 401 สำหรับ `/api/auth/me`** อีกต่อไป
- เมื่อขอ OTP ควรเห็น: `✅ OTP email sent to [email]`

### 3. ทดสอบ OTP
1. กรอกอีเมลในฟอร์ม signup
2. กดปุ่ม "ขอ OTP"
3. ควรเห็น toast: `✅ OTP ส่งไปที่อีเมลของคุณแล้ว`
4. ตรวจสอบอีเมลเพื่อรับ OTP

## ⚠️ Errors ที่ยังเห็น (เป็นเรื่องปกติ):

### 1. WebSocket Errors
```
WebSocket connection to 'wss://...' failed
```
- **เป็นเรื่องปกติ** สำหรับ production environment
- WebSocket ใช้สำหรับ real-time updates (ไม่จำเป็นสำหรับ OTP)

### 2. Vite HMR Errors
```
[vite] failed to connect to websocket
```
- **เป็นเรื่องปกติ** เมื่อรัน production build
- Vite HMR ใช้สำหรับ development hot-reload

### 3. 401 Error สำหรับ `/api/auth/me`
- **เป็นเรื่องปกติ** เมื่อยังไม่ได้ login
- ตอนนี้จะไม่แสดงใน console อีกต่อไป (แก้ไขแล้ว)

## 📝 Checklist:

- [x] API timeout เพิ่มเป็น 10 วินาที (global)
- [x] OTP request timeout เพิ่มเป็น 15 วินาที
- [x] ลด error logging สำหรับ expected 401
- [x] Gmail App Password อัพเดทเป็น 16 ตัวอักษร
- [x] OTP email ส่งสำเร็จ

## 🎉 ผลลัพธ์:

- ✅ OTP System ทำงานได้แล้ว
- ✅ Error logging สะอาดขึ้น
- ✅ Frontend ไม่แสดง error ที่ไม่จำเป็น
- ✅ Timeout เพียงพอสำหรับการส่ง email


# 🔐 สร้าง Gmail App Password ใหม่

## ⚠️ ปัญหา: App Password ไม่ถูกต้อง

จาก test script พบว่า:
- Password ปัจจุบันมี **15 ตัวอักษร** (`fovygwijlrddube`)
- Gmail App Password ควรมี **16 ตัวอักษร** (ไม่มี space)

## ✅ วิธีแก้ไข:

### ขั้นตอนที่ 1: ตรวจสอบ 2-Step Verification

1. ไปที่: https://myaccount.google.com/security
2. ตรวจสอบว่า **2-Step Verification** เปิดอยู่หรือไม่
3. ถ้ายังไม่ได้เปิด → เปิดใช้งานก่อน

### ขั้นตอนที่ 2: สร้าง App Password ใหม่

1. **ไปที่ App Passwords**
   - ไปที่: https://myaccount.google.com/apppasswords
   - หรือ: Google Account → Security → 2-Step Verification → App passwords

2. **สร้าง App Password**
   - เลือก **App**: Mail
   - เลือก **Device**: Other (Custom name)
   - ใส่ชื่อ: `iCAS CMU HUB`
   - กด **Generate**

3. **คัดลอก Password**
   - จะได้ password **16 ตัวอักษร** (ไม่มี space)
   - ตัวอย่าง: `abcd efgh ijkl mnop` → ใช้เป็น `abcdefghijklmnop`
   - **สำคัญ:** คัดลอกทันที เพราะจะแสดงแค่ครั้งเดียว!

### ขั้นตอนที่ 3: อัพเดทไฟล์ `.env`

1. เปิดไฟล์ `backend/.env`
2. แก้ไขบรรทัด `SMTP_PASS`:
   ```env
   SMTP_PASS=your_new_16_character_app_password
   ```
   (แทนที่ `your_new_16_character_app_password` ด้วย App Password ใหม่)

3. บันทึกไฟล์

### ขั้นตอนที่ 4: ทดสอบอีกครั้ง

```powershell
cd backend
npm run test:smtp
```

**ผลลัพธ์ที่คาดหวัง:**
```
✅ SMTP connection successful!
   Your Gmail credentials are working correctly.
```

### ขั้นตอนที่ 5: Restart Server

```powershell
# หยุด server (Ctrl+C)
npm run dev
```

## 🔍 ตรวจสอบว่าแก้ไขสำเร็จ:

เมื่อขอ OTP ควรเห็น:
```
✅ OTP email sent to [email]
```

แทนที่จะเป็น:
```
❌ Error sending OTP email: Error: Invalid login: 535
```

## 📝 Checklist:

- [ ] 2-Step Verification เปิดอยู่
- [ ] สร้าง App Password ใหม่แล้ว (16 ตัวอักษร)
- [ ] อัพเดท SMTP_PASS ใน `.env` แล้ว
- [ ] รัน `npm run test:smtp` แล้วผ่าน
- [ ] Restart server แล้ว
- [ ] ทดสอบขอ OTP แล้วสำเร็จ

## ⚠️ หมายเหตุ:

- App Password จะเป็น **16 ตัวอักษร** (ไม่มี space)
- Password ที่มี 15 ตัวอักษร = ไม่ใช่ App Password จริง
- App Password จะแสดงแค่ครั้งเดียว → ต้องคัดลอกทันที
- ถ้าลืม → ต้องสร้างใหม่


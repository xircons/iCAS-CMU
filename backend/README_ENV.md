# 🔧 การตั้งค่า .env File

## สรุปปัญหาและวิธีแก้ไข

### ❌ ปัญหาที่พบ:
1. **Error: Missing credentials for "PLAIN"** - SMTP credentials ไม่ถูกโหลด
2. **Error 401: No token provided** - เป็นเรื่องปกติเมื่อยังไม่ได้ login (ไม่ใช่ปัญหา)
3. **Error 429: กรุณารอ 1 นาทีก่อนขอ OTP ใหม่** - Rate limiting ทำงานปกติ

### ✅ วิธีแก้ไข:

#### 1. ตรวจสอบไฟล์ `.env`
```powershell
# ตรวจสอบว่าไฟล์มีอยู่
cd backend
Test-Path .env

# ดูเนื้อหา SMTP
Get-Content .env | Select-String "SMTP"
```

#### 2. สร้างไฟล์ `.env` ใหม่ (ถ้ายังไม่มีหรือมีปัญหา)

**วิธีที่ 1: ใช้ PowerShell Script**
```powershell
cd backend
.\create-env.ps1
```

**วิธีที่ 2: สร้างด้วยมือ**
```powershell
cd backend
@"
# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=12345
DB_NAME=icas_cmu_hub

# Gmail SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=icascmu@gmail.com
SMTP_PASS=fovygwijlrddube
SMTP_FROM=iCAS CMU HUB <icascmu@gmail.com>
"@ | Out-File -FilePath .env -Encoding utf8
```

#### 3. Restart Server
หลังจากสร้างหรือแก้ไขไฟล์ `.env` แล้ว:
```powershell
# หยุด server (Ctrl+C)
# แล้วรันใหม่
npm run dev
```

### 🔍 ตรวจสอบว่าแก้ไขสำเร็จ:

เมื่อ server เริ่มทำงาน คุณควรเห็น:
```
✅ Database connected successfully
✅ email_otps table exists
```

และเมื่อขอ OTP ควรเห็น:
```
✅ OTP email sent to [email]
```

แทนที่จะเป็น:
```
❌ Error sending OTP email: Missing credentials
```

### 📝 หมายเหตุ:

- ไฟล์ `.env` ถูก gitignore เพื่อความปลอดภัย
- ต้องสร้างไฟล์นี้ทุกครั้งที่ clone repository ใหม่
- อย่า commit ไฟล์ `.env` ที่มี credentials จริง
- ถ้ายังมีปัญหา ให้ตรวจสอบว่า:
  - ไฟล์ `.env` อยู่ใน `backend/` directory
  - มี SMTP_USER และ SMTP_PASS ในไฟล์
  - ไม่มี space หรือ special characters ที่ไม่ถูกต้อง


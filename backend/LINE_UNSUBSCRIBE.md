# LINE Bot - คำสั่งและเมนู

## 📋 เมนูคำสั่ง

LINE Bot รองรับคำสั่งต่อไปนี้:

### 🔔 สมัครรับแจ้งเตือน
**คำสั่ง:**
- `สมัคร`
- `ลงทะเบียน`
- `subscribe`
- `register`

**คำอธิบาย:** ลงทะเบียนรับแจ้งเตือนกิจกรรมและงานจากชมรม

---

### 📊 ตรวจสอบสถานะ
**คำสั่ง:**
- `สถานะ`
- `ตรวจสอบ`
- `status`

**คำอธิบาย:** ตรวจสอบสถานะการรับแจ้งเตือนและข้อมูล email ที่ลงทะเบียน

---

### ❌ ยกเลิกการรับแจ้งเตือน
**คำสั่ง:**
- `ยกเลิก`
- `ยกเลิกการรับแจ้งเตือน`
- `unsubscribe`
- `stop`

**คำอธิบาย:** ยกเลิกการรับแจ้งเตือน (สามารถสมัครใหม่ได้ในภายหลัง)

---

### ℹ️ ความช่วยเหลือ
**คำสั่ง:**
- `help`
- `ช่วยเหลือ`
- `เมนู`

**คำอธิบาย:** แสดงเมนูคำสั่งทั้งหมด

---

## วิธียกเลิกการรับแจ้งเตือน

### วิธีที่ 1: ผ่าน LINE Bot (แนะนำ)

1. **เปิด LINE App** และไปที่ chat กับ LINE Official Account
2. **พิมพ์คำสั่ง**:
   - `ยกเลิก`
   - `ยกเลิกการรับแจ้งเตือน`
   - `unsubscribe`
   - `stop`
3. **Bot จะตอบกลับ**:
   ```
   ✅ ยกเลิกการรับแจ้งเตือนสำเร็จ
   
   📧 Email: your-email@cmu.ac.th
   
   คุณจะไม่ได้รับแจ้งเตือนอีกต่อไป
   
   หากต้องการรับแจ้งเตือนอีกครั้ง พิมพ์ "สมัคร" ได้เลย
   ```

### วิธีที่ 2: Block/Unfollow LINE Official Account

1. **Block หรือ Unfollow** LINE Official Account
2. ระบบจะลบ conversation state อัตโนมัติ
3. **หมายเหตุ**: ข้อมูล LINE User ID ยังคงอยู่ใน database (สำหรับ re-subscription)

### วิธีที่ 3: ใช้ Script (สำหรับ Admin)

```bash
# ลบ LINE user โดยใช้ email
npm run remove:line-user user@cmu.ac.th
```

หรือ:

```bash
tsx src/scripts/remove-line-user.ts user@cmu.ac.th
```

## วิธีลงทะเบียนใหม่

หลังจากยกเลิกแล้ว หากต้องการรับแจ้งเตือนอีกครั้ง:

**วิธีที่ 1: ใช้คำสั่ง**
1. **พิมพ์ "สมัคร"** ใน LINE chat
2. **กรอก email** ของคุณ
3. Bot จะยืนยันการลงทะเบียนอีกครั้ง

**วิธีที่ 2: Add Friend ใหม่**
1. **Add Friend** LINE Official Account อีกครั้ง
2. Bot จะแสดงเมนูคำสั่ง
3. **พิมพ์ "สมัคร"** และกรอก email

## ตรวจสอบสถานะ

### ดู LINE users ที่ลงทะเบียนแล้ว

```sql
SELECT 
  lu.email,
  u.first_name,
  u.last_name,
  lc.state,
  lu.created_at
FROM line_users lu
LEFT JOIN users u ON lu.email = u.email
LEFT JOIN line_conversations lc ON lu.line_user_id = lc.line_user_id;
```

### ดูเฉพาะ users ที่ active (state = 'completed')

```sql
SELECT 
  lu.email,
  u.first_name,
  u.last_name,
  lu.created_at
FROM line_users lu
LEFT JOIN users u ON lu.email = u.email
INNER JOIN line_conversations lc ON lu.line_user_id = lc.line_user_id
WHERE lc.state = 'completed';
```

## หมายเหตุ

- เมื่อยกเลิกแล้ว ข้อมูลจะถูกลบจาก `line_users` และ `line_conversations`
- หาก Block/Unfollow ข้อมูล `line_users` จะยังคงอยู่ (สำหรับ re-subscription)
- สามารถลงทะเบียนใหม่ได้ตลอดเวลา


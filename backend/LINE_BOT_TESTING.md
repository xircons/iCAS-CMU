# LINE Bot Testing Guide

## ✅ ขั้นตอนการทดสอบ

### 1. ทดสอบการ Add Friend และลงทะเบียน

1. **เปิด LINE App** บนมือถือ
2. **ค้นหา LINE Official Account** ของคุณ (ใช้ Channel ID หรือ QR Code จาก LINE Developers Console)
3. **Add Friend** (กดปุ่ม "เพิ่มเพื่อน")
4. **ตรวจสอบข้อความแรก**
   - Bot ควรส่งข้อความ: "สวัสดีครับ! 👋\n\nต้องการรับแจ้งเตือนกิจกรรมและงานจากชมรมหรือไม่?"
   - ถ้าไม่ได้รับข้อความ ตรวจสอบ server logs

5. **ทดสอบการลงทะเบียน**
   - ตอบ "ใช่" หรือ "yes"
   - Bot ควรถาม: "กรุณากรอกอีเมลของคุณ (เช่น somying@cmu.ac.th)"
   - กรอก email ที่มีในระบบ (เช่น `leader@cmu.ac.th`, `member@cmu.ac.th`)
   - Bot ควรตอบ: "✅ ลงทะเบียนสำเร็จ!\n\nคุณจะได้รับแจ้งเตือนกิจกรรมและงานจากชมรมที่คุณเป็นสมาชิกผ่าน LINE นี้"

### 2. ทดสอบการส่งแจ้งเตือนกิจกรรม

1. **Login เข้าระบบ** ด้วย account ที่ลงทะเบียน LINE Bot แล้ว
2. **สร้างกิจกรรมใหม่**
   - ไปที่ Calendar หรือ Club Calendar
   - สร้างกิจกรรมใหม่ (เช่น "ทดสอบ LINE Bot")
   - ตั้งค่าวันที่ เวลา สถานที่
   - Save

3. **ตรวจสอบ LINE**
   - ควรได้รับข้อความแจ้งเตือนใน LINE
   - ข้อความควรมีข้อมูล: ชื่อกิจกรรม, ประเภท, วันที่, เวลา, สถานที่

### 3. ทดสอบการส่งแจ้งเตือนงาน

1. **Login เข้าระบบ** ด้วย account ที่ลงทะเบียน LINE Bot แล้ว
2. **สร้างงานใหม่**
   - ไปที่ Assignment Center
   - เลือกชมรม
   - สร้างงานใหม่
   - ตั้งค่าวันที่เปิดรับส่ง และกำหนดส่ง
   - Save

3. **ตรวจสอบ LINE**
   - ควรได้รับข้อความแจ้งเตือนใน LINE
   - ข้อความควรมีข้อมูล: ชื่องาน, วันที่เปิดรับส่ง, กำหนดส่ง, คะแนนเต็ม (ถ้ามี)

## 🔍 ตรวจสอบ Database

### ตรวจสอบว่าลงทะเบียนสำเร็จ

```sql
-- ดู LINE users ที่ลงทะเบียนแล้ว
SELECT * FROM line_users;

-- ดู conversation states
SELECT * FROM line_conversations;
```

### ตรวจสอบว่า user เป็นสมาชิกชมรม

```sql
-- ดูสมาชิกชมรม (ต้อง status = 'approved')
SELECT 
  u.email,
  c.name as club_name,
  cm.status,
  cm.role
FROM club_memberships cm
JOIN users u ON cm.user_id = u.id
JOIN clubs c ON cm.club_id = c.id
WHERE cm.status = 'approved';
```

## 🐛 Troubleshooting

### ไม่ได้รับข้อความเมื่อ Add Friend

1. **ตรวจสอบ server logs**
   - ดูว่ามี error หรือไม่
   - ตรวจสอบว่า LINE webhook ถูกเรียกหรือไม่

2. **ตรวจสอบ LINE Developers Console**
   - ไปที่ Messaging API settings
   - ดู Webhook delivery status
   - ตรวจสอบว่า webhook events ถูกส่งมาหรือไม่

3. **ตรวจสอบ environment variables**
   ```bash
   # ใน server console ควรเห็น:
   ✅ LINE Bot client initialized
   ```

### ไม่ได้รับแจ้งเตือนเมื่อสร้างกิจกรรม/งาน

1. **ตรวจสอบว่า user ลงทะเบียน LINE Bot แล้ว**
   ```sql
   SELECT * FROM line_users WHERE email = 'your-email@cmu.ac.th';
   ```

2. **ตรวจสอบว่า user เป็นสมาชิกชมรม**
   ```sql
   SELECT * FROM club_memberships 
   WHERE user_id = (SELECT id FROM users WHERE email = 'your-email@cmu.ac.th')
   AND status = 'approved';
   ```

3. **ตรวจสอบ server logs**
   - ดูว่ามี error เมื่อส่ง LINE notification หรือไม่
   - ควรเห็น log: "Error sending LINE notifications for event" ถ้ามีปัญหา

4. **ตรวจสอบว่า LINE User ID ถูกต้อง**
   ```sql
   SELECT line_user_id FROM line_users WHERE email = 'your-email@cmu.ac.th';
   ```

### LINE Bot ไม่ตอบกลับ

1. **ตรวจสอบ webhook URL**
   - ต้องเป็น HTTPS (ไม่ใช่ HTTP)
   - URL ต้องถูกต้อง: `https://your-domain.com/api/line/webhook`

2. **ตรวจสอบ LINE middleware**
   - ถ้า webhook signature ไม่ถูกต้อง จะ reject request
   - ตรวจสอบ LINE_CHANNEL_SECRET ถูกต้องหรือไม่

3. **ตรวจสอบ server logs**
   - ดู error messages
   - ตรวจสอบว่า webhook endpoint ถูกเรียกหรือไม่

## 📝 Checklist

### Setup
- [ ] Environment variables ถูกตั้งค่าแล้ว
- [ ] Database tables ถูกสร้างแล้ว (line_users, line_conversations)
- [ ] Server restart แล้ว
- [ ] Webhook URL verify สำเร็จแล้ว

### Testing
- [ ] Add Friend แล้ว Bot ส่งข้อความต้อนรับ
- [ ] ตอบ "ใช่" แล้ว Bot ถาม email
- [ ] กรอก email แล้ว Bot ยืนยันการลงทะเบียน
- [ ] สร้างกิจกรรมแล้วได้รับแจ้งเตือนใน LINE
- [ ] สร้างงานแล้วได้รับแจ้งเตือนใน LINE

### Database
- [ ] มี record ใน line_users
- [ ] มี record ใน line_conversations (state = 'completed')
- [ ] User เป็นสมาชิกชมรม (status = 'approved')

## 🎯 Next Steps

หลังจากทดสอบสำเร็จแล้ว:

1. **แจ้งสมาชิกชมรม** ให้ Add LINE Official Account
2. **ทดสอบกับสมาชิกหลายคน** เพื่อดูว่าแจ้งเตือนส่งถึงทุกคนหรือไม่
3. **ตรวจสอบ performance** - ถ้ามีสมาชิกเยอะ อาจต้อง optimize
4. **เพิ่ม features** (ถ้าต้องการ):
   - คำสั่งอื่นๆ (เช่น "ยกเลิกการรับแจ้งเตือน")
   - แจ้งเตือนล่วงหน้า (reminder)
   - สถิติการใช้งาน


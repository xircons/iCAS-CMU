# 🔧 แก้ไข Gmail Error 535

## ❌ ปัญหา: Error 535 - Username and Password not accepted

แม้ว่าไฟล์ `.env` จะถูกต้องแล้ว แต่ยังได้ Error 535 ซึ่งหมายความว่า:

**App Password ที่ใช้อยู่ (`fovygwijlrddube`) อาจจะ:**
1. ไม่ใช่ App Password จริง (อาจเป็น password ปกติ)
2. หมดอายุแล้ว
3. ถูก revoke แล้ว
4. ไม่ได้สร้างจาก Gmail account ที่ถูกต้อง

## ✅ วิธีแก้ไข:

### 1. ทดสอบ SMTP Configuration

รันคำสั่งนี้เพื่อตรวจสอบว่า credentials ถูกต้องหรือไม่:

```powershell
cd backend
npm run test:smtp
```

### 2. สร้าง App Password ใหม่

ถ้า test ล้มเหลว ให้สร้าง App Password ใหม่:

1. **เปิดใช้งาน 2-Step Verification** (ถ้ายังไม่ได้เปิด)
   - ไปที่: https://myaccount.google.com/security
   - เปิดใช้งาน **2-Step Verification**

2. **สร้าง App Password ใหม่**
   - ไปที่: https://myaccount.google.com/apppasswords
   - เลือก **App**: Mail
   - เลือก **Device**: Other (Custom name)
   - ใส่ชื่อ: `iCAS CMU HUB`
   - กด **Generate**
   - **คัดลอก password ที่ได้** (16 ตัวอักษร ไม่มี space)

3. **อัพเดทไฟล์ `.env`**
   ```powershell
   # เปิดไฟล์ backend/.env
   # แก้ไข SMTP_PASS เป็น App Password ใหม่
   SMTP_PASS=your_new_16_character_app_password
   ```

4. **ทดสอบอีกครั้ง**
   ```powershell
   npm run test:smtp
   ```

5. **Restart Server**
   ```powershell
   # หยุด server (Ctrl+C)
   npm run dev
   ```

## 🔍 ตรวจสอบว่าแก้ไขสำเร็จ:

เมื่อรัน `npm run test:smtp` ควรเห็น:

```
✅ SMTP connection successful!
   Your Gmail credentials are working correctly.
```

เมื่อขอ OTP ควรเห็น:

```
✅ OTP email sent to [email]
```

## ⚠️ หมายเหตุสำคัญ:

- **App Password ต้องเป็น 16 ตัวอักษร** (ไม่มี space)
- **ต้องสร้างจาก Gmail account ที่ถูกต้อง** (icascmu@gmail.com)
- **2-Step Verification ต้องเปิดอยู่**
- **App Password จะแสดงแค่ครั้งเดียว** - ต้องคัดลอกทันที

## 📝 Checklist:

- [ ] 2-Step Verification เปิดอยู่
- [ ] สร้าง App Password ใหม่แล้ว
- [ ] อัพเดท SMTP_PASS ใน `.env` แล้ว
- [ ] รัน `npm run test:smtp` แล้วผ่าน
- [ ] Restart server แล้ว
- [ ] ทดสอบขอ OTP แล้วสำเร็จ

